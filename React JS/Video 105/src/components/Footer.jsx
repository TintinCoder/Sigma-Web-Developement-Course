import PropTypes from 'prop-types'
import React, { Component } from 'react'

export class Footer extends Component {
    static propTypes = {}

    render() {
        return (
            <footer>
                <h1 style={{position: 'sticky', top: 0, left
            : 0}}>This is a freaking footer</h1>
            </footer>
        )
    }
}

export default Footer